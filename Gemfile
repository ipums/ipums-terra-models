ruby '2.0.0'
source "https://rubygems.org"

# Declare your gem's dependencies in terrapop_models.gemspec.
# Bundler will treat runtime dependencies like base dependencies, and
# development dependencies will be added by default to the :development group.
gemspec

# Declare any dependencies that are still in development here instead of in
# your gemspec. These might include edge Rails or gems from your path or
# Git. Remember to move these dependencies to your gemspec before releasing
# your gem to rubygems.org.

gem 'rails', '~> 4.2.6'
gem 'mime-types-data'
gem 'jdbc-postgres', '>= 9.3.1102'
gem 'activerecord-jdbcmysql-adapter'
gem 'activerecord-jdbcpostgresql-adapter'
gem 'builder'
gem 'nokogiri'
gem 'chronic_duration'
gem 'user_management_api', git: 'git@github.umn.edu:mpc/user_management_api.git', tag: 'v0.0.15'
gem 'terrapop_uma', git: 'git@github.umn.edu:mpc/terrapop_uma.git'

gem 'rspec'
gem 'factory_girl_rails'
gem 'pry'
gem 'faraday' # For connecting to Tractor API to submit Extract Requests
gem 'delayed_job_active_record'
